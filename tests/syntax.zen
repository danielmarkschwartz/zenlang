include "my/path/library"
include "my/path/library" myident

typedef mytype int32
typedef mytype1 type1
typedef mytype2 mod1->type2
typedef mytype3 *int32
typedef mytype4 []int32
typedef mytype5 [1]uint16
typedef mytype6 func (void) void
typedef mytype7 func (int, float, []*uint64) int
typedef mytype8 func (void) (*[]int, **float)
typedef mytype9 func (void) []*void

struct struct0 {x,y,z float32}

struct struct1 {
    data uint8;
    capacity,   //takes int type of following member
    i,          //takes int type of following member 
    len int;
}

typedef struct2 struct {
    data uint8;
    capacity, i, len int;
}

enum enum0 {MON, TUE, WED, THUR, FRI, SAT, SUN}
enum enum1 {MON=1, TUE, WED, THUR, FRI, SAT, SUN=0}
enum enum2 {MON, TUE, WED, THUR, FRI, SAT, SUN mytype1}
typedef enum3 enum {MON, TUE, WED, THUR, FRI, SAT, SUN}
